{
  "name": "api-sdk",
  "version": "1.0.0",
  "description": "Cross-platform SDK that abstracts calls to both flat file storage (series of JSON files) and an OTA API",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix src/js",
    "build:js": "npm run build --prefix src/js",
    "test": "npm run build --prefix src/js && node utils/js/usage.js",
    "test:js": "npm run build --prefix src/js"
  },
  "keywords": [
    "api",
    "sdk",
    "ota",
    "json"
  ],
  "author": "API SDK Team",
  "license": "MIT"
}

